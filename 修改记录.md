# Claude Code 接入修改记录

- 新增 Claude External CLI 配置解析与校验，支持 `claude`、MCP 注入、工作目录和环境变量校验。
- 在 Team 装配阶段挂载 Claude External Member，不加入原生成员角色列表。
- 增加 Leader Coding 路由规则：Coding 请求优先创建或复用 `claude-coder`，禁止回落到 `swarmflow` 原生工作流。
- 完善 Claude Persona，要求使用 `read_inbox`、`view_task`、`claim_task`、`send_message`、`member_complete_task` 汇报协作过程。
- 前端补充 External CLI 成员、任务状态、生命周期、工具调用、成员消息和历史恢复展示。
- Claude 成员展示名统一为 `Claude Code`；内部成员 ID 仍使用 `claude-coder`。
- 修复群聊/成员事件的 Claude 元数据补全，支持展示 `role_type=external_cli` 和 `cli_agent=claude`。
- Team 配置启用进程模式、pyzmq、SQLite；关闭 `enable_swarmflow`，避免 Coding 请求被原生工作流接管。
- 为每个 Team Session 分配稳定独立的 pyzmq 端口，修复第二个 Prompt 或新会话因 `Address in use` 无响应的问题。
- 增加 Claude、Team MCP、任务生命周期和前端展示相关的测试覆盖；前端生产构建和后端语法检查通过。
